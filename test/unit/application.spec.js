describe('marionettist application', function() {
  'use strict';

  it("should pass an error if no target is passed", function(done) {
    var hey = true;
    expect(hey).to.be.equal(true);
    done()
  });

});
